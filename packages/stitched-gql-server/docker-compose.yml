version: '3.9'
services:
  graphql_stitched:
    build: .
    tty: true
    environment:
      - PORT=10000
    ports:
      - '10000:10000'
    restart: unless-stopped
    logging:
      driver: 'json-file'
      options:
        max-size: '15m'
        max-file: '3'
