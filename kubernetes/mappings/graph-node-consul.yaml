apiVersion: getambassador.io/v2
kind: Mapping
metadata:
  name: subgraph-node
  # namespace: ambassador
spec:
  prefix: /api/subgraph/
  # rewrite: /api/subgraph/subgraphs/name/niftier/composable-contract
  service: subgraph-node-sidecar-proxy
  resolver: consul-dc1
  tls: ambassador-consul
  load_balancer:  # underscored in their docs
    policy: round_robin
