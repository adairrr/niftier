---
apiVersion: consul.hashicorp.com/v1alpha1
kind: ServiceIntentions
metadata:
  name: intentions-to-subgraph-ipfs
spec:
  destination:
    name: subgraph-ipfs
  sources:
    - name: subgraph-node
      action: allow
